import { colors as c, sizes as s, linGradCoords as lgc } from "./app.styles";

const profilePictureStyle = {
    gradient: {
        color: c.primaryGradientScheme,
        coords: lgc.var1
    },
    sr: {
        size: s.w2,
        radius: s.w4
    }
};

export { profilePictureStyle };